const accountId=144535

let AccoutName='Ani'
let accountPassword='123'
accountCity='Mumbai' //can be declared this way also but not a good practice

/* var is not used in the js anymore 
due to block isse */
console.log(AccoutName);
console.table([accountId,accountPassword,AccoutName,accountCity])